package info_interface;

import java.util.Map;

import vo.Info;

public interface InfoMapper {
	
	public int insertInfo(Info info);
	public intupdateInfo(Map<String, Integer>map);
	public int insertInfo(Info info);
	public int insertInfo(Info info);

}
